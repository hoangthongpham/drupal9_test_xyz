<?php
  function post_module_theme($existing, $type, $theme, $path) {
    return [
      'post_template' => [
        'variables' => ['test_var' => NULL],
      ],
      'show_template' => [
        'variables' => ['test_var' => NULL],
      ],
    ];
  }
function post_module_page_attachments(array &$page)
{
  $page['#attached']['library'][] = 'post_module/post-lib';
  //dd($page);
}

?>
